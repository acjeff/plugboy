/// @description  Init rope
gravX = 2;
gravY = 2;
ropeLen = false;
knotNum = false;
knotDist = false;
lineSpriteWidth = false; // this 4 is here because i have X origin moved a little

/* more iterations means better precision 
   and stronger connections between knots but it's slower to compute
*/
constraintsIterations = false; 

//if (ropeLen && knotNum && lineSpriteWidth && constraintsIterations) {
//	// build initial state of rope
	
